---
branch: feature/dashboard-redesign (from main @ d30bcc2)
task: dashboard-redesign-implementation
status: in_progress
last_updated: 2026-03-01T13:15:00Z
---

# Dashboard Redesign — Implementation Handoff

## Current State

All 11 plan tasks committed. Self-hosting done. **Visueller Mockup-Vergleich abgeschlossen: CSS ist 1:1 Kopie des Mockups — keine Fixes nötig.**
**Nächster Schritt: views.css Modul-Refactoring + main.css.bak löschen.**

## Completed

- **Task 1**: tokens.css (commit 0df74d1)
- **Task 2**: components.css (commit 8498fac)
- **Task 3**: main.css split → main.css (795) + views.css (2501) + modals.css (112) (commit 1199292)
- **Task 4**: index.html head (Outfit font, PWA meta, CSS stack) (commit c52b0d7)
- **Task 9**: settings.html shared tokens + legacy aliases (commit c017df2)
- **Task 5**: Layout shell (mobile header, compact sidebar, bottom nav, JS) (commit d736aae)
- **Task 6**: Hero card redesign + issues panel (commit 44bcaa3)
- **Task 7**: Metrics grid, health rings, channel tables (commit 816fa17)
- **Task 8**: JS updates (theme-color meta, swipe sidebar, switchView global) (commit 1a28301)
- **Task 10**: PWA (sw.js, manifest, /sw.js route) (commit 4020cbb)
- **Fix**: closeMobileSidebar → closeSidebar (commit 75c14de)
- **Self-hosting**: All CDN deps local (fonts, lucide, chart.js) + logo fix (commit c75c315)
- **Mockup-Vergleich**: CSS-Implementierung verifiziert — exakte 1:1 Kopie aller Tokens, Regeln, Media-Queries

## Open Items

1. **views.css Modul-Refactoring** — Analyse komplett (siehe unten). Module-spezifische Styles aus views.css in `app/modules/<name>/static/style.css` verschieben. Für jedes Modul: `static/` Verzeichnis anlegen, `style.css` erstellen, `has_css: true` in `manifest.json` hinzufügen.
2. **main.css.bak löschen** — Original-Monolith-Backup nach Verifikation entfernen.

## views.css Refactoring-Plan (Analyse fertig)

### Was raus muss (→ Modul-CSS):

| Modul | Ziel-Datei | Zeilen in views.css | ~Zeilen |
|-------|-----------|---------------------|---------|
| speedtest | `app/modules/speedtest/static/style.css` | 4-131 | ~125 |
| bqm | `app/modules/bqm/static/style.css` | 308-447, 2433-2501 | ~210 |
| journal | `app/modules/journal/static/style.css` | 452-591, 1086-1178, 1529-2347 | ~950 |
| bnetz | `app/modules/bnetz/static/style.css` | 654-661 | ~8 |

### Was bleibt in views.css (~1200 Zeilen):
- **Shared**: sort-indicator (133), error-box (135-138), waiting/spinner (140-148), info-tip (1180-1232), demo-badge (1235-1246), info-icon (1487-1511), issue-badge (1513-1526)
- **Core-Views**: channels (150-160, 2350-2430), trends (162-306), events (593-653 + 663-872), correlation (875-1083), gaming (1248-1480)
- **Legacy**: hero-sep (1482-1484, display:none — kann gelöscht werden)

### Achtung:
- `.btn-small`, `.btn-accent`, `.btn-muted` (1991-2015) sind generische Button-Utilities im Journal-Block — bleiben in views.css als shared
- `.table-card` (664-692) ist im Events-Block definiert aber auch von Correlation genutzt — bleibt in views.css
- Keines der 4 Module hat aktuell ein `static/` Verzeichnis — muss angelegt werden
- `manifest.json` braucht kein `has_css` Feld — der Loader prüft `mod.has_css` was aus dem Dateisystem kommt (Existenz von `static/style.css`)
- Zeile 158-160: CSS-Syntax-Fehler (orphaned closing brace) — beim Refactoring fixen

## v2-dev Container

- Running on `http://localhost:8767` (DEMO_MODE=true)
- Rebuild: `docker compose -f docker-compose.dev.yml build && docker compose -f docker-compose.dev.yml up -d`

## Key Files

- **Plan**: `docs/plans/2026-03-01-dashboard-redesign-plan.md`
- **Design Spec**: `docs/plans/2026-03-01-dashboard-redesign-design.md`
- **Mockup**: `mockup-dashboard.html` (ground truth — CSS verified matching)
- **CSS**: `app/static/css/` — fonts.css, tokens.css, components.css, main.css, views.css, modals.css
- **Fonts**: `app/static/fonts/` — Outfit, JetBrains Mono, Inter (all woff2)
- **Vendor**: `app/static/vendor/` — lucide.min.js, chart.umd.min.js, chartjs-adapter-date-fns
- **HTML**: `app/templates/index.html`, `settings.html`, `setup.html`
- **JS**: `app/static/js/hero-chart.js`, `app/static/sw.js`
- **Backup**: `app/static/css/main.css.bak` (original 4101-line monolith, delete after verification)

## Resume Instructions

```
Dashboard Redesign fortsetzen. Branch: feature/dashboard-redesign, Worktree: .worktrees/dashboard-redesign
Lies .continue-here.md für den vollen Kontext.

Mockup-Vergleich erledigt (CSS = 1:1 Kopie). Offene Punkte:
1. views.css Modul-Refactoring: Detaillierter Plan in .continue-here.md. Styles aus views.css
   in 4 Modul-Dateien verschieben (speedtest ~125, bqm ~210, journal ~950, bnetz ~8 Zeilen).
   Für jedes Modul: static/ Verzeichnis + style.css anlegen. index.html lädt Module-CSS
   automatisch via Jinja-Loop (mod.has_css prüft Existenz von static/style.css).
2. main.css.bak löschen.
Container: localhost:8767. Rebuild: docker compose -f docker-compose.dev.yml build && up -d
```
